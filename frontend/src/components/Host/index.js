import { useState } from 'react';

import './HostExperience.css';

const CreateListings = () => {

    return (
        <h1>this is the page where you create a new listing</h1>
    );
};

export default CreateListings;